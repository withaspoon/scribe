# Defaults for scribe initscript
# sourced by /etc/init.d/scribe
# installed at /etc/default/scribe by the maintainer scripts

#
# This is a POSIX shell fragment
#

# Additional options that are passed to the Daemon.
SCRIBE_CONFIG="/etc/scribe/scribe.conf"
DAEMON_OPTS="-c $SCRIBE_CONFIG"

